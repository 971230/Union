var moduleADDs = {
  var me=this;
  var idArray = [];
		if(goods && goods.length>0){
			goods.each(function(idx,item){
			idArray.push(item.value);
		});
  }
};
$(function(){
  moduleADDs.init();
});
   